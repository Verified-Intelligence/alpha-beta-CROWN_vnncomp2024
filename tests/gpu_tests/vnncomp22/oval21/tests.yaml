tests:
  # These tests should all return SAT. If you regenerate the references, make sure
  # this is still the case. Previously, the third test incorrectly returned UNSAT.
  - idx: 14
    args: --pgd_order before --complete_verifier mip
  - idx: 14
    args: --pgd_order skip --complete_verifier mip
  - idx: 14
    args: --pgd_order skip --complete_verifier mip --prune_after_crown